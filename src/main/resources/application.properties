# Global Settings
# Railway requires ${PORT} to bind the app to their network
server.port=${PORT:8081}
spring.profiles.active=mysql

# POSTGRES (Aiven)
# This block is only read if spring.profiles.active=postgres
#spring.config.activate.on-profile=postgres
#spring.datasource.url=jdbc:postgresql://ofs-db-26-ofs-banking-2026.j.aivencloud.com:13225/defaultdb?ssl=require
#spring.datasource.username=avnadmin
#spring.datasource.password=AVNS_TLW_-5U5P_U6T-qeqHs
#spring.datasource.driver-class-name=org.postgresql.Driver
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.hibernate.ddl-auto=update

# MYSQL (Local / Railway)
spring.config.activate.on-profile=mysql
# We use variables here so Railway can connect to your cloud DB
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/ofs_db?createDatabaseIfNotExist=true}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:IamIronman}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update

# =========================================================
# COMMON CONFIGURATIONS (Always Active)
# =========================================================

# MongoDB Atlas Configuration
# Reference the variable MONGO_URI in Railway
spring.data.mongodb.uri=${MONGO_URI:mongodb+srv://it24ganeshprabhu_db_user:5X3K3nFp9osMV2xe@adhaar-demo.zmfs8mq.mongodb.net/Adhaar-demo?retryWrites=true&w=majority}

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=online.financial.services.000@gmail.com
# Use GMAIL_APP_PASS in Railway Variables
spring.mail.password=${GMAIL_APP_PASS:gije risd pjsa xnhw}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# File Limits
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
server.tomcat.max-swallow-size=10MB

# AI API Key
# Use GROQ_API_KEY in Railway Variables
groq.api.key=${GROQ_API_KEY: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}
